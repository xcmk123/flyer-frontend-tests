export default function DynamicArrayRender() {
  return (
    <p className="text-center">
      Add "add", "remove" actions in the sidebar and "resize" action of a box in the "Improve array
      render" question. "Resize" a box should not re-render all boxes.
    </p>
  );
}
